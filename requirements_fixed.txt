# Configuración específica para evitar conflictos de TensorFlow/Keras
# Estas versiones están verificadas como compatibles entre sí

tensorflow==2.13.0
keras==2.13.1
deepface==0.0.75

# Dependencias core que deben mantenerse estables
streamlit==1.32.0
numpy==1.26.4
pillow==10.2.0
pandas==2.2.0
opencv-python-headless==4.8.1.78
supabase==2.0.0
python-dotenv==1.0.0

# Dependencias específicas de DeepFace (para evitar conflictos)
protobuf==3.20.3
tqdm
gdown
